## Packages
framer-motion | Smooth animations for result cards and list items
lucide-react | Icons for UI elements
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
- The backend accepts `POST /api/classifications` with `FormData` containing an `image` file.
- The backend returns the detected shape and container color.
- We need to poll or refresh the list after upload.
